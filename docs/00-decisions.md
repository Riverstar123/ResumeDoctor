# 项目基线与决策记录

文档基线：1.0。接口契约版本：0.1.0。交付截止：2026 年 11 月 10 日前，内部交付检查日期为 11 月 9 日。

## 项目约定

| 项目 | 约定 |
|---|---|
| 团队 | A–E 五个角色，各自负责模块和验证，A 协调集成 |
| 仓库 | https://github.com/Riverstar123/ResumeDoctor；单仓库、短分支、PR 审核 |
| 开工规则 | 五人完成协作演练后进入公共骨架；之后按各 Issue 依赖开工，无需重复确认 |
| 前端 | React + TypeScript + Vite；npm 与 package-lock.json |
| 后端 | Python + FastAPI + Pydantic；SQLAlchemy 与 Alembic |
| 架构 | 模块化单体，HTTP API 与独立 worker 共用业务模块和 PostgreSQL |
| 数据 | 不可变 Profile/JD 内容版本，JSONB 快照、数据库任务队列 |
| 身份 | 匿名 HttpOnly cookie，会话七天过期；MVP 不做注册和跨设备找回 |
| 输入 | 文字 PDF、DOCX 或自然语言对话；文件 10 MiB、20 页上限 |
| 岗位 | 每次 1–5 个 JD，公司信息只作补充背景 |
| 算法 | coverage-v1 + matcher-v1，AI 不直接决定最终分数 |
| 模型 | 默认 fake；统一网关，真实模式由服务端配置，支持选择多个模型 |
| 真实接入 | E 实现适配器、配置模板与说明；组长在本地填写实际端点、模型与密钥，E 验证 |
| 交付 | 可在本地复现的 Web 应用、真实 AI 调用及双模型切换证据、演示材料 |
| 范围 | 不含公网生产部署、OCR、自动投递、招聘方筛选和批量候选人排名 |

版本号由 SET-01 在工程配置中锁定；这是一项开发交付，不是团队开工前的额外审批。依赖、运行命令以合并后的锁文件和 README 为准，不使用不受控的 latest。

## 开发阶段规则

协作演练是 #2–#6。完成对应 PR 并合并后关闭任务。五项全部验收后 A 开始 SET-01；公共骨架合并后其他成员按依赖并行开发。创建 Issue 不代表已实现；关闭 Issue 必须附验收结果。
当前规划整理只修改文档和任务，不代替团队完成演练或业务开发。应用实现进度以 GitHub Issues、PR 和可执行测试为准。

## 变更管理

需求、接口、数据库和算法变化必须在同一 PR 说明原因、影响模块、兼容策略与验证，相关消费者审核。业务未知信息仍须保留 unknown，不能为使文档“确定”而编造简历事实。

| 基线 | 内容 |
|---|---|
| 0.1 | 建立需求、契约、数据库、评分和五模块说明 |
| 1.0 | 固定团队约定，完整任务依赖和里程碑；演练完成后按条件直接开工 |
