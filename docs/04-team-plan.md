# 五人分工与协作计划

## 成员与职责

| 角色 | 主责 | 交付物 | 第一评审人 |
|---|---|---|---|
| A | M2 平台与集成；GitHub 带教 | API 路由、会话隔离、数据库、任务与集成 | C；共享契约由相关成员共同看 |
| B | M1 前端 | 输入、档案/JD 编辑、进度、报告、交互测试 | E |
| C | M3 输入与结构化 | 文件解析、对话建档、JD 提取、字段证据 | D |
| D | M4 评分 | 条件匹配、规则引擎、分项解释、固定测试样例 | C |
| E | M5 模型网关与报告；验收协调 | 假/真模型网关、结构化报告、Markdown 导出、验收记录 | B；网关由 C 加看 |

这不是“一个人写代码、四个人写文档”：每个人都拥有一块可运行模块并负责自己的验证。A 的集成工作较多，首轮先交付最小会话和假任务；E 负责组织全组验收来平衡工作量。

## 共享文件

- OpenAPI、数据库、contracts DTO、根配置：A 整理，变更影响谁就由谁评审。
- 评分规则：D 维护，C/E 校验语义。
- UI 全部由 B 维护。其他人提接口与展示需求，不直接大改页面。
- 数据库迁移只由 A 编号合并，避免多人生成冲突迁移。
- 各成员维护自身模块文档和测试；任何人发现契约冲突先记录，不自行改另一模块来“适配”。

## 阶段目标与开工规则

| 阶段 | 目标完成日期 | 交付 |
|---|---|---|
| P0 协作演练 | 2026-09-30 | A–E 各一个 PR，互相审核并合并 |
| P1 工程与假数据闭环 | 2026-10-11 | 公共骨架、独立模块入口、fake 全链路 |
| P2 核心功能与持久化 | 2026-10-25 | 两种输入、多 JD、数据库/worker、评分与报告 |
| P3 质量与真实 AI 验收 | 2026-11-05 | 回归/端到端通过，真实模型与双模型切换 |
| P4 演示与最终交付 | 2026-11-09 | 可复现环境、全组彩排、最终交付检查 |

课程要求在 11 月 10 日前交付；以上是团队阶段目标，不表示任务已经完成。
五项演练全部验收后 A 开始 SET-01；骨架合并后 B/C/D/E 与 A 的模块任务并行。每人按 [任务索引](team/backlog.md) 找到前置已完成的下一项，不需要再次申请开工。
依赖任务必须有已合并成果和验收记录。`status:blocked` 不会自动移除，负责人核对后更新标签。每人同时只推进一个主要任务，等待依赖期间准备样例或审核队友 PR。

## 每日协作

每人用 3 句话更新 Issue：完成了什么、下一步是什么、被谁/哪个契约阻塞。需他人配合时链接 Issue 和字段，避免只在私聊约定。
PR 保持一个用户可验收变化；先 Draft PR 让队友看到方向。第一次每人先完成一个小文档 PR，走通同步、分支、提交、推送、评审、合并。

## Definition of Done

需求验收满足；接口/字段未漂移；模块测试与必要联调通过；异常状态有处理；无真实个人资料/密钥；对应文档更新；有队友评审；合并后 main 可运行（实现阶段）；Issue 记录验证证据后关闭。
